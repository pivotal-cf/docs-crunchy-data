**Release Date:** July 14, 2017

Security fixes included in this release:

* Prevent the tile from logging manifests generated by the broker when deploying PostgreSQL,
  because they contain sensitive information.

